<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.taonce.myjetpack">

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".navigation.NavigationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".livedata.LiveDataActivity">
        </activity>
        <activity android:name=".data.binding.observable.ObservableActivity">
        </activity>
        <activity android:name=".data.binding.function.FunctionActivity">
        </activity>
        <activity android:name=".data.binding.expression.ExpressionActivity">
        </activity>
        <activity android:name=".data.binding.list.ListActivity">
        </activity>
        <activity android:name=".data.binding.DataBindingActivity">
        </activity>
        <activity android:name=".KTXActivity">
        </activity>
    </application>

</manifest>